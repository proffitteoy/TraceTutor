# TraceTutor 仓库协作约定

## 1. 回答语言与规则优先级

- 默认使用中文回答，除非用户明确要求英文。
- 规则优先级从高到低：
  1. 用户当前消息里的明确要求
  2. 当前仓库根目录或更近目录下的 `AGENTS.md`
  3. 当前仓库 `docs/` 内与任务直接相关的设计文档
  4. 当前仓库 `README.md`、目录说明、脚本说明
  5. 通用模板或外部 skills
- 不得因为存在 skill 而覆盖本仓库的业务边界和设计事实。

## 2. 仓库现状与项目事实

- 当前仓库是 **文档先行的初始化阶段仓库**，不是可直接运行的成品项目。
- 当前确认的业务边界：
  - `apps/iris`：前端交互层
  - `apps/api`：自有 API / 本地 Agent Runtime / 工具服务层
  - `db/pgsql`：题目资产库脚本与迁移
  - `db/sqlite`：用户学习状态库脚本与迁移
  - `docs/`：架构、数据模型、冷启动与专项规则
- 当前仓库 **没有真实构建文件、启动命令、测试命令或可执行代码**。不得虚构 `npm`、`pnpm`、`python`、迁移命令；只有在对应组件真正落地后，才能把真实命令写入文档。

## 3. 修改前必须阅读的上下文

- 修改前先读取与目标相关的文档，而不是直接套模板。
- 默认优先阅读：
  - [README.md](./README.md)
  - [docs/架构设计.md](./docs/架构设计.md)
  - [docs/接口与闭环设计.md](./docs/接口与闭环设计.md)
  - [docs/agent设计.md](./docs/agent设计.md)
  - [docs/pgsql设计.md](./docs/pgsql设计.md)
  - [docs/SQLite设计.md](./docs/SQLite设计.md)
- 若任务只涉及流程规范、冷启动或目录边界，再补读：
  - [docs/冷启动.md](./docs/冷启动.md)
  - [docs/代码组织.md](./docs/代码组织.md)
  - [docs/skills.md](./docs/skills.md)

## 4. 设计红线

- 不要让 `Iris` 直接访问数据库。
- 不要让本地 Agent Runtime 或模型 API 直接写数据库。
- 不要暴露“万能 SQL”接口给 Agent；Agent 只能输出查询计划，真实 SQL 由 API 层生成和校验。
- 不要把题目资产和用户学习状态混存：
  - `PgSQL` 只存稳定题目资产、相似关系、知识点与方法资产。
  - `SQLite` 只存用户学习行为、掌握状态、复习调度、运行时上下文与日志。
- 不要把未审核题目直接进入正式召回链路；草稿、导入题、AI 生成题必须保留质量状态。

## 5. 目录边界

- `apps/iris`
  - 放前端应用代码、前端组件、渲染契约、页面状态与交互测试。
- `apps/api`
  - 放对 Agent 暴露的安全 API、规则校验、状态写回、查询计划执行器。
- `apps/api/src/agent`
  - 放本地 Prompt、Workflow、模型 API 适配、工具调度和 Agent 输出校验。
- `db/pgsql`
  - 放题目资产表、迁移、种子数据、召回查询与审核相关脚本。
- `db/sqlite`
  - 放用户状态表、迁移、本地运行态与状态引擎相关脚本。
- `docs/`
  - 放长期有效的设计与协作文档；如果实现落地后文档失真，优先修正文档。
- `runtime/`
  - 放本地运行期生成物，不要把正式源码长期堆在这里。

## 6. 变更原则

- 优先做最小可验证修改，不擅自扩张为大规模重构。
- 当前阶段优先落地：
  - 项目级契约
  - 真实目录边界
  - README / 文档入口
  - 数据与 Agent 的边界说明
- 如果开始实现某个组件，先在对应目录补该组件自己的 `README` 或说明文件，再写代码。
- 如果发现设计文档之间冲突，先在 `docs/` 内收敛冲突，再推进实现。

## 7. 文档、测试与示例同步

- 每次改动后检查是否需要同步更新：
  - 根 `README.md`
  - 对应目录的 `README.md`
  - `docs/` 中受影响的设计文档
  - 示例 JSON、接口契约、状态字段说明
- 当前仓库没有实际测试入口时，不要伪造“已测试”。应明确说明未运行测试，以及原因。
- 一旦某个组件拥有可运行命令，必须把最小启动、测试、格式化命令写到该组件目录说明和根 README。

## 8. 何时查看 skills

- 普通文档整理、代码修复、接口开发、数据库建模，默认不需要额外看 skill。
- 以下场景按需查看：
  - 前端视觉设计或高保真页面：`frontend-design`
  - 本地网页联调：`webapp-testing`
  - PostgreSQL 建模与查询：`postgresql`
  - OpenAI 官方产品文档：`.system/openai-docs`
- 是否查看 skill，始终以本仓库设计事实为先。

## 9. 最终汇报要求

- 最终回复至少说明：
  - 改了什么
  - 为什么这样改
  - 如何验证
  - 剩余风险或未覆盖项
- 如果是仓库初始化或冷启动，还要额外说明：
  - 依据哪些现有文档得出项目约束
  - 删除或改写了哪些通用模板内容
  - 新增了哪些目录、说明或占位物
