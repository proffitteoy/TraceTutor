# API 目录

这里放 TraceTutor 的自有服务端接口与工具执行层。

职责边界：

- 接收 Agent 产出的查询计划
- 将查询计划校验后转成安全数据库查询
- 负责状态写回、规则校验、日志记录和降级策略
- 屏蔽底层 PgSQL / SQLite 实现细节

禁止事项：

- 不要向 Agent 暴露万能 SQL 接口
- 不要绕过规则层直接把 Agent 的 `state_delta` 写入正式状态表

后续真实实现时，需补：

- API 框架选型
- 接口契约
- 运行与测试命令
